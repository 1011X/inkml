pub(crate) trait Node {
    /*
    fn append<T>(&mut self, _: T)
    where Self: Sized, T: Node;
    
    fn assign<T, U>(&mut self, _: T, _: U)
    where Self: Sized,
        T: Into<String>,
        U: Into<Value>;
    */
}
